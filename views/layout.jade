block precompile
	- bodyClass = []
	- breadcrumbs = [{ title: 'Home', url: path.to.home() }]

html
	head
		title Abootay
		meta(name="viewport", content="width=device-width, user-scalable=yes")

		block scripts
			include js/lib/index
			include js/abootay
			include css/reset
			include css/all

	body(class=bodyClass)
		.root-container
			block body_content